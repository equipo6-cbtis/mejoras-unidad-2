//calcular el area de un rectangulo
#include <stdio.h>
#include <conio.h>
int main (void)
{
int base, altura,area;
printf ("introduce el valor de la base");
scanf ("%d", &base);
printf ("introduce el valor de la altura");
scanf ("%d", &altura);
 area=base*altura;
printf ("el resultado del area de un rectangulo es %d",area );	
	
	
	
}

// Mejora el area de un rectangulo
#include <stdio.h>
#include <conio.h>

int main (void)
{
	float base,altura,area;
	printf ("ingresa la base");
	scanf ("%f", &base);
	printf ("ingresa la altura");
	scanf ("%f", &altura);
	
	area=base*altura;
	
		printf ("el area es %f", area);
		if (area<100)
		{
			printf ("el rectangulo es chico");
		}
		else
		if (area>100)
		{
			printf ("el rectangulo es de gran cantidad");
		}
	
}

